{
  "name": "@ramp/payments-mcp",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Stub MCP server exposing mcp__payments__pay_vendor. Enforcement lives in the PreToolUse hook, not here.",
  "bin": {
    "ramp-payments-mcp": "dist/server.js"
  },
  "exports": {
    ".": "./dist/server.js"
  },
  "scripts": {
    "build": "tsc -b",
    "typecheck": "tsc -b",
    "lint": "tsc -b",
    "pretest": "tsc -b",
    "test": "node --test \"test/**/*.test.ts\"",
    "start": "node dist/server.js"
  },
  "dependencies": {
    "@ramp/shared": "workspace:*",
    "@modelcontextprotocol/sdk": "^1.0.0",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "typescript": "^5.7.3"
  }
}
