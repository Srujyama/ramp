{
  "name": "@ramp/payments-mcp",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Self-enforcing MCP server exposing mcp__payments__pay_vendor. Drives the shared requestPurchase lifecycle (policy → provenance → proof → persist → verify → sandbox execute), fail-closed.",
  "bin": {
    "ramp-payments-mcp": "dist/server.js"
  },
  "exports": {
    ".": "./dist/server.js"
  },
  "scripts": {
    "build": "tsc -b",
    "typecheck": "tsc -b",
    "lint": "tsc -b",
    "pretest": "tsc -b",
    "test": "node --test \"test/**/*.test.ts\"",
    "start": "node dist/server.js"
  },
  "dependencies": {
    "@ramp/shared": "workspace:*",
    "@ramp/ledger": "workspace:*",
    "@ramp/gate": "workspace:*",
    "@modelcontextprotocol/sdk": "^1.0.0",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "typescript": "^5.7.3"
  }
}
