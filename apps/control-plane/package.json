{
  "name": "@ramp/control-plane",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "DEMO-ONLY control plane: live pricing + (later) UI-triggered REAL gated transactions via requestPurchase + typed input-table admin. NOT the audit bridge, NOT the gate.",
  "exports": {
    ".": "./dist/server.js"
  },
  "scripts": {
    "build": "tsc -b",
    "typecheck": "tsc -b",
    "lint": "tsc -b",
    "pretest": "tsc -b",
    "test": "node --test \"test/**/*.test.ts\"",
    "start": "node dist/server.js"
  },
  "dependencies": {
    "@ramp/ledger": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "typescript": "^5.7.3"
  }
}
