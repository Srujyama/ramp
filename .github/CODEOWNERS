# CODEOWNERS — Provable Agent Spend
# Every PR touching a path below requests a review from its owner.
# Owners: @Srujyama (owner), @neilporw, @JonKach, @tomasciar.
#
# Split of the 9 workspaces:
#   @Srujyama  — the gate (hero) + the three security pillars + the shared contract + the client SDK + repo wiring
#   @neilporw  — the ledger/registry fact source + the self-enforcing payments MCP tool
#   @JonKach   — the dashboard audit console
#   @tomasciar — see the fallback; no area claimed yet
# Later, more specific rules win, so the fallback comes first.

# Fallback: the owner reviews anything not otherwise claimed.
*                           @Srujyama

# --- Shared contract + the hero gate + hook (owner) -------------------------
/packages/shared/           @Srujyama
/packages/gate/             @Srujyama
/.claude/                   @Srujyama
/hook/                      @Srujyama

# --- The security pillars (owner) ------------------------------------------
# Pillar 2 (provenance), 3 (quarantine), 4 (attestation). Grouped with the gate
# because they ARE the enforcement path: a change here changes what gets paid.
/packages/provenance/       @Srujyama
/packages/quarantine/       @Srujyama
/packages/attestation/      @Srujyama
/packages/client/           @Srujyama

# --- The demo/audit drivers (owner) ----------------------------------------
# scripts/demo.mjs is executable documentation of PITCH.md and runs in CI.
/scripts/                   @Srujyama

# --- Authoritative facts + the spend seam (neil) ---------------------------
/packages/ledger/           @neilporw
/apps/payments-mcp/         @neilporw

# --- Dashboard shell (jon) -------------------------------------------------
/apps/dashboard/            @JonKach

# --- Repo infra: owner keeps CI + CODEOWNERS + root config -----------------
/.github/                   @Srujyama
/package.json               @Srujyama
/pnpm-workspace.yaml        @Srujyama
/tsconfig.base.json         @Srujyama
